## .env.example
# Copy this file to ".env" and fill sensitive values. Do NOT commit ".env".

# PostgreSQL (application)
POSTGRES_DB=tech_news_db
POSTGRES_USER=tech_admin
POSTGRES_PASSWORD=changeme

# Ports
POSTGRES_PORT=15432
BACKEND_PORT=18080
FRONTEND_PORT=13000

<<<<<<< HEAD
# Backend API: http://localhost:18080 (Health: http://localhost:18080/health)
BACKEND_PORT=18080

# Airflow Web UI: http://localhost:18088
AIRFLOW_PORT=18088

# PostgreSQL Host Port: localhost:15432
POSTGRES_PORT=15432

# Cấu hình cào báo VnExpress (Step 1)
CRAWL_URLS=https://vnexpress.net/so-hoa/cong-nghe
# One or more article URLs separated by commas.
# Leave empty for manual-only mode; an empty list disables the scheduled DAG.
=======
# Crawler / app config
>>>>>>> 9f7b726 (fix file config)
CRAWL_URLS=
CRAWL_SOURCE_ID=1
CRAWL_SOURCE_NAME=vnexpress
CRAWL_TIMEOUT_SECONDS=20
CRAWL_SCHEDULE=0 */6 * * *

# Airflow Postgres
AIRFLOW_POSTGRES_DB=airflow
AIRFLOW_POSTGRES_USER=airflow
AIRFLOW_POSTGRES_PASSWORD=changeme_airflow

# Airflow web and admin
AIRFLOW_PORT=18088
AIRFLOW_SECRET_KEY=replace_with_strong_secret
AIRFLOW_ADMIN_USERNAME=admin
AIRFLOW_ADMIN_PASSWORD=change_me_admin
AIRFLOW_ADMIN_EMAIL=admin@example.com

# Optional: encryption key for Airflow (FERNET)
# FERNET_KEY=

TZ=Asia/Ho_Chi_Minh
